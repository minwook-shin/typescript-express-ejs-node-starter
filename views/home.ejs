<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@1.0.0/css/bulma.min.css">
    <script src="https://unpkg.com/htmx.org@1.9.12"></script>
</head>
<body>
    <section class="section">
        <div class="container">
            <h1 class="title">
                <%= title %>
            </h1>
            <p class="subtitle">
                Welcome to the <strong>Modern TypeScript Express EJS Starter</strong>.
            </p>
            
            <div class="box">
                <h2 class="subtitle">HTMX Interaction</h2>
                <button class="button is-primary" hx-get="/?name=HTMX" hx-target="#result" hx-swap="innerHTML">
                    Click me
                </button>
                <div id="result" class="mt-4"></div>
            </div>
        </div>
    </section>
</body>
</html>